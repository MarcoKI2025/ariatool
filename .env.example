# ARIA Configuration
# Copy this file as .env and fill in your values

# Optional: Set a password to gate access (leave empty for open demo mode)
VITE_APP_PASSWORD=

# Optional: Supabase integration (only needed for multi-device sync)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
